--- 
artifacts: 
  discard-path: true
  files: 
    - target/order.service.jar
phases: 
  build: 
    commands: 
      - "echo entered the build phase."
      - "mvn clean package"
  post_build: 
    commands: 
      - "echo build ccompleted succcessfully"
version: 0.2
